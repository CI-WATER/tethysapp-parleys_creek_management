To install:

1. run install on setup script:

python setup.py install

2. Initialize the database

tethys syncstores parleys_creek_management

3. Ensure there is a CKAN dataset service called "default" setup on the Tethys instance

